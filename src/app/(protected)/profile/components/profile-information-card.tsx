"use client"

import { useUserContext } from "@/contexts/user-context"

type Props = {

}

export const ProfileInformationCard: React.FC<Props> = ({ }) => {
  const { user } = useUserContext()

  return (
    <div>

    </div>
  )
}